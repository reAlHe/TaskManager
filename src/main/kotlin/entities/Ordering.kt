package entities

/**
 * entities.Ordering representing the mechanism how to sort the managed processes.
 *
 * @constructor Create empty entities.Ordering
 */
enum class Ordering {
    PID, TIME, PRIORITY
}